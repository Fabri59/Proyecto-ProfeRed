@model Proyecto_Red.Models.Mision

@{
    ViewData["Title"] = "Nueva Misión";
}

<div class="mb-4">
    <h1 class="display-6 fw-bold">Nueva Misión</h1>
    <p class="text-muted">Describe la misión del plan estratégico TI.</p>
</div>

<form asp-action="CreateMision" method="post">
    <div class="mb-3">
        <label asp-for="Descripcion" class="form-label">Misión</label>
        <textarea asp-for="Descripcion" class="form-control" rows="5"></textarea>
        <span asp-validation-for="Descripcion" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-success">
        <i class="fas fa-save me-2"></i> Guardar misión
    </button>
    <a asp-action="Estrategia" class="btn btn-secondary ms-2">Cancelar</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
